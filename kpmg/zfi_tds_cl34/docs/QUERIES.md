# ZFI_TDS_CL34 - open points

Build contract deliverable 6. Every `" ASSUMPTION:` marker in the source has a row
here, and so does every deliberate departure from the FS's own stated derivation
(Q13-Q15). Ranked: Q1-Q3 change a number on a compliance report and are the ones to
put to the functional side first.

Object: `ZFI_TDS_CL34` | FS: Clause 34 TDS Report FS.xlsx v1, 21/08/2026
Owners: **Ankita Parikh** (functional, FI/TDS) | **Bhavin Suthar** (MM / account determination)

| # | Owner | Column / area | Question | What the build does today |
|---|---|---|---|---|
| Q1 | Ankita | Y - Cumulative Amount | `FIWTIN_ACC_EXEM` is keyed by `SECCO` as well, so a vendor accumulates once per section code. Must column Y show the accumulation of the document's OWN section code, or the vendor's latest accumulation irrespective of section code? | Latest `WT_DATE` not after the upper posting-date bound. Section code is **not** a restriction; where two rows share that date the document's own section code wins the tie. Rows are never summed - the table already accumulates. |
| Q2 | Ankita | U/V/W/X - Exemption certificate | `FIWTIN_TAN_EXEM` is keyed by `SECCODE` **and** `FIWTIN_TANEX_SUB`. Must these four columns show the certificate of the document's own section code (`BSEG-SECCO`), or the vendor's latest certificate irrespective of section code? | Valid on the posting date (`WT_EXDF <= BUDAT <= WT_EXDT`) for the item's own tax type and tax code; then latest `WT_EXDF`; then the document's own `SECCODE` as tie-break, then the sub-indicator. Neither is a filter. |
| Q3 | Bhavin | F/G - GL Code, normal-PO branch | Is a valuation grouping code active (OMWM) for company codes 1000 and 4000? | `T030` is read on `KTOPL` + `KTOSL = 'BSX'` + `BKLAS` only - `T001K-BWMOD` could not be verified on this landscape. The blank grouping code / blank account modifier entry wins; where none exists the lowest one is shown and the document is counted in the run's status message. If a grouping code IS active, `T001K-BWMOD` must be verified in SE11 and pushed into the `T030` key. |
| Q4 | Ankita | W - Threshold Applicability | FS heading [W7] reads "Threshold Applicability (Y/N)" but FS description [W6] asks for the threshold AMOUNT maintained in the vendor master. The FS contradicts itself. Should the heading become "Threshold Amount", or the content a Y/N flag? | The **amount** (`FIWTIN_TAN_EXEM-FIWTIN_EXEM_THR`) is shown under the FS's own unchanged heading, so the contradiction stays visible. Blank = no threshold maintained. |
| Q5 | Ankita | J - Nature of Payment | FS [J6] says the HEADER text, FS [J2] says `BSEG-SGTXT` of the line "where LIFNR <> blank". Which is it? | `SGTXT` of the **vendor line**, per FS [J2] and build contract D5. The item number the withholding item carries is tried first; if that line is not a vendor line (`KOART = 'K'` and `LIFNR` filled) the lowest-numbered vendor line of the document is used. Documents with no vendor line at all are counted in the status message, never dropped in silence. |
| Q6 | Ankita | Whole list | Reversed (`BKPF-STBLG` filled) and parked (`BKPF-BSTAT`) documents - include or exclude? The FS is silent. | All included. Both fields exist and are readable; add the filter as soon as there is a decision. |
| Q7 | Ankita | Row granularity | One row per withholding-tax item, i.e. per `BUKRS`/`BELNR`/`GJAHR`/`BUZEI`/`WITHT` of `WITH_ITEM`. Confirm this matches "GL wise and document number wise". | As stated. Rows carrying neither a base amount nor a tax amount are dropped (build contract D5). |
| Q8 | Ankita / Basis | P/T/W/Y - amount columns | Amount columns carry no ALV currency reference; `CL_SALV_COLUMN_LIST=>SET_CURRENCY_COLUMN` is UNVERIFIED on this release. Confirm the method exists in SE24. | The four amounts render with two decimals and no currency. `WAERS` (`T001-WAERS`) is carried in the row and set technical. If the method exists, wire it up; if not, either type the amount fields from a DDIC structure that carries the reference or drop the `WAERS` component. **Assumes every company code in scope reports in INR.** |
| Q9 | Ankita | H/I - Section, Section Code Description | **RESOLVED on the FS's half — `T059Z-TXT40` does not exist.** Proved on the Astral system, activation 27/08/26: *"No component exists with the name TXT40"*. T059Z has no text field at all. **Still open for Ankita:** column I now shows `T059OT-TEXT40`, the text of the *official withholding tax key* — which is what column H holds, so H and I agree. If the business means the text of the tax *code* instead, it is `T059ZT-TEXT40` (key `SPRAS`/`LAND1`/`WITHT`/`WT_WITHCD`). Which one? | `T059OT-TEXT40`, key `SPRAS`/`LAND1`/**`WT_QSCOD`** (note: `QSCOD` on T059Z, `WT_QSCOD` on T059OT). If `QSCOD` is unmaintained in config, H **and** I are blank together — configuration, not a defect. Two places to change if it moves: `FETCH_TAX_CONFIG` and the col I read in `BUILD_OUTPUT`. |
| Q10 | Ankita / Basis | E and U-Y | Is `LFA1-J_1IPANNO` populated for the vendors in scope? A blank PAN blanks column E **and** U/V/W/X/Y together. | Read as CHAR 40 (never CHAR 10). Scattered blanks are normal; a column-wide blank is a defect to investigate. |
| Q11 | Basis / Ankita | F/G - GL Code, direct-FI branch | Is `BSEG-GHKON` populated on the `KTOSL = 'WIT'` line for direct FI postings on Astral? The field exists (DD03L position 364) but its content is unverified. | Used as the offsetting GL. If it is empty in practice, check `BSEG-GKONT` then `WITH_ITEM-HKONT_OPP` - it is a one-field switch. No heuristic guess is made; F and G stay blank and the document is counted in the status message. |
| Q12 | Basis / Ankita | Data source | **Do `I_WithholdingTaxItem` and `I_JournalEntry` exist on this release, with the element names the FS uses?** The FS names both views and the report reads them. Four element names are the FS's own (`ACCOUNTINGDOCUMENT`, `CUSTOMERSUPPLIERACCOUNT`, `WITHHOLDINGTAXCODE`, `WHLDGTAXBASEAMTINCOCODECRCY`, `WHLDGTAXAMTINCOCODECRCY`, `WITHHOLDINGTAXPERCENT`); four more the FS never spells out are assumed to follow standard naming (`CompanyCode`, `FiscalYear`, `AccountingDocumentItem`, `WithholdingTaxType`). None can be checked without the system. **This is the first thing to find out — a wrong element name here stops activation.** Check in SE11 / ADT: does the view exist, and does its field list match? | The two views, inner-joined, under the mandatory filter. The whole read is isolated in **one form, `FETCH_WT_ITEMS`** — if the views are absent or thin, the equivalent `WITH_ITEM` + `BKPF` select is written out verbatim in that form's comment block, ready to swap in. Nothing downstream touches a view. |
| Q16 | Basis / Ankita | Authorisation | Because of Q12 the report reads base tables, which carry no authorisation of their own, so an explicit check was added on **`F_BKPF_BUK`** (`BUKRS`, `ACTVT` = `03`). Is that the object in force on this landscape for company-code-level display of FI documents, and is `03` the right activity? Confirm in **SU21** (object exists, fields `BUKRS` + `ACTVT`) and **SU24** for the transaction, once one is assigned. | `CHECK_AUTHORISATION` checks every company code the selection resolves to and reports the first one the user may not display. Called from `VALIDATE_SELECTION` (dialog, `TYPE 'E'`) **and** from `START-OF-SELECTION`, because `AT SELECTION-SCREEN` does not run in a background job. If the object is wrong the report refuses every company code and nobody can run it — this is the first thing to check if users report "no display authorisation". |
| Q13 | Ankita | X - Certificate Number | FS [X7] spells the heading "Ceritificate Number". The report ships it corrected to "Certificate Number". Confirm, or the FS heading is restored verbatim. | Heading reads "Certificate Number"; all other 24 headings are the FS wording unchanged, including [W7] "Threshold Applicability (Y/N)" - see Q4. |
| Q14 | Bhavin | GLCode tab - the RMRP branch test | **The one place the FS is not implemented as written, and the FS itself is the evidence.** The tab says "For records having AWKEY as RMRP", but two rows below it says "Get AWKEY and provide first 10 digits of AWKEY in BELNR of RSEG and Fiscal year in GJAHR". One field cannot both equal `RMRP` and hold a 10-digit invoice number plus a 4-digit year. `RMRP` is the reference *transaction* = `BKPF-AWTYP`; `AWKEY` is the reference *key* the same tab then cuts the invoice number out of. Confirm. | Tested on **`AWTYP`**. Implementing the literal wording would match no document at all: every PO-based invoice would silently fall to the direct-FI branch and column F would be wrong for most of the report **with nothing failing visibly** — which is why this one was not left literal. |
| Q17 | Ankita | K/M - Document Date, Invoice Date | FS [K2]/[M2] read the dates from `BSEG-H_BUDAT` / `H_BLDAT`. These are header dates propagated onto the line item and can be blank depending on how the document was posted. Is that acceptable, or should they come from `BKPF-BUDAT` / `BLDAT`? | As the FS says, off the vendor line. Note the knock-on: `H_BUDAT` also drives the exemption-certificate validity test and the cumulative cut-off, so if it is blank, **columns K, U, V, W, X and Y all go blank together**. `BKPF` is already buffered, so this is a one-line change if needed. |
| Q18 | Ankita / Basis | G - GL Name | FS [G2] says verbatim: provide `"ASTL"` in `KTOPL`. It is the only hardcoded value in the report. Do company codes 1000 **and** 4000 both run chart of accounts ASTL? Check SE16 on `T001`, field `KTOPL`. | Hardcoded to `ASTL` as instructed, via the named constant `GC_KTOPL_GL`. A company code on a different chart returns a **blank** column G, never a wrong description. Everywhere else the chart is read from `T001` per company code, because the GLCode tab asks for exactly that. |
| Q19 | Bhavin | GLCode tab - MBEW read | FS [H27]: `BWKEY = RSEG-WERKS`. That holds under plant-level valuation. Is valuation at plant level for these company codes (OX14 / `T001K`)? | Implemented as the FS says - the plant is passed as the valuation area. Under company-code-level valuation the `MBEW` read finds nothing and column F is blank for stock purchase orders. |
| Q20 | Ankita | Account type | FS [C2] asks for `CUSTOMERSUPPLIERACCOUNT` without qualifying it, so **no account-type filter is applied** and customer withholding items are not excluded. Should they be? | Not excluded. Where a customer item occurs, columns D and E come back blank because `LFA1` has no such account - visible, not silent. Add `KOART = 'K'` to `FETCH_WT_ITEMS` if the answer is yes. |
| Q15 | Ankita | Selection screen defaults | `INIT_DEFAULTS` proposes an April-March period and a matching fiscal year. Do company codes 1000 and 4000 run an April-March variant (V3), or a calendar-year variant (K4)? | April-March assumed. Under K4 the proposed `P_GJAHR` and the proposed posting-date range contradict each other (the two filters are ANDed, so January-to-March would drop out) and the user must overtype both. Proposal only - both fields are guarded by `IS INITIAL`, so a variant or `SUBMIT ... WITH` always wins. |


---

## Validated on the first live run — 27/08/26

Company code 1000, FY 2026, 49 rows returned. Arithmetic reconciled on a sample:
`4,500 x 2% = 90`, `96,666.66 x 2% = 1,933.33`, `22,321.43 x 0.1% = 22.32`,
`200,000 x 1% = 2,000`. Row 19 (`6,000,000` base, `1,000` tax) reconciles once the
5,000,000 threshold is applied: `(6,000,000 - 5,000,000) x 0.1% = 1,000` — the exemption
logic works.

### Closed by the run

| # | Was | Evidence |
|---|---|---|
| Q9 | is `T059OT` the right text table? | **YES.** Section and description populate throughout (`194C` / `TDS on Payment to Contractors`). `T059Z-TXT40` is dead and buried. |
| Q10 | is `LFA1-J_1IPANNO` populated? | **YES.** Column E filled on every row. |
| Q11 | is `BSEG-GHKON` populated? | **YES.** GL and GL name derive for direct FI postings. The `GKONT` / `HKONT_OPP` fallbacks are not needed. |
| Q14 | `AWTYP` vs the FS's literal `AWKEY` | **`AWTYP` was right.** `5110000001` returns `33010001 STOCK OF RAW MATERIALS`, `5110000011-13` return `33010012 STOCK OF FINISHED GOODS` — the RMRP branch resolves through RSEG -> MBEW -> T030 BSX. The literal `AWKEY = 'RMRP'` would have left every one of these blank, silently. |
| Q17 | is `BSEG-H_BUDAT` populated? | **YES.** Columns K and M filled on every row; no need to fall back to BKPF. |
| Q18 | do both company codes run chart `ASTL`? | **Confirmed for 1000.** Column G resolves. 4000 still untested. |
| Q19 | is valuation at plant level? | **YES.** The BKLAS -> T030 route returns accounts, so `BWKEY` = `WERKS` holds. |

### Raised by the run — need a functional answer

| # | Owner | Area | Question | Today |
|---|---|---|---|---|
| Q21 | Ankita | P/T - sign | Base and TDS come out **negative** (`4,500.00-`, `90.00-`) because `WITH_ITEM` stores vendor credits that way. On a compliance report that gets totalled, positive figures are normally wanted. Flip them? **If yes, it must be `x -1`, NOT `ABS( )`** - the second run showed a credit memo (doc `5110000002`, transaction `MR8M`) coming through as `+450.00 / +0.45` against the original invoice's `-450.00 / -0.45`. The sign carries real meaning: taking absolute values would turn that reversal into a second deduction and the column would over-total. | Raw `WT_QSSHH` / `WT_QBSHH` as stored. One-line change in `BUILD_OUTPUT`. |
| Q22 | Ankita | Row selection | 7 rows / 6 documents carry **no deduction at all**: `1700000025-28` (down-payment *requests* — noted items, nothing posted) and `1900000087-88`. The FS objective says "documents in which TDS is deducted". Exclude rows with zero tax amount, and/or exclude noted items (`BKPF-BSTAT`)? | Both included; build contract D5 keeps a row when base **or** tax is non-zero. The status line reports them rather than dropping them silently. Related to Q6. |
| Q23 | Ankita | J - Nature of Payment | Column J is **blank on roughly 80% of rows**. It is `BSEG-SGTXT` per FS [J2], but FS [J6] said *header* text. The blankness is evidence the business meant `BKPF-BKTXT`. Which? | `SGTXT` of the vendor line. This is Q5 with data behind it now. |
| Q24 | Ankita | R/S - the two rate columns | R ("as per section") is `T059Z-QSATZ`; S ("deducted") is `WithholdingTaxPercent`, which is **also** `QSATZ` — so the two are near-duplicates by the FS's own design. Worse, **rows 27, 28, 29 and 38 (LEAN SERVICE) do not reconcile**: rate shows `2.0000` but the amounts imply 0.1% (`1,000 -> 1.00`, `10,000 -> 10.00`, `3,900 -> 3.90`), while rows 36/37 for the same vendor reconcile at 2%. Partial exemption? If column S should be the rate **actually applied**, the candidates are `TDS / base` or `WITH_ITEM-WT_QSZRT` (the exemption rate, DE `WT_EXRT`). | Both columns show `QSATZ`, one from the document and one from config. |
| Q25 | Ankita | W/Y - zero vs blank | Threshold and Cumulative render as `0.00` where no certificate exists, which reads as "zero threshold" rather than "none maintained". Combined with Q4 (heading still says `(Y/N)`), how should "not maintained" look? | Numeric `0.00`. Suppressing zeros is an ALV setting, not a logic change. |

| Q26 | Ankita | Row selection — item status | The driver now excludes `I_WithholdingTaxItem-WhldgTaxItemStatus` in `'V'`, `'D'`, `'M'`, `'S'` (instruction of 07/09/26). Three things need confirming: (a) is that list complete, or are there further non-reportable codes; (b) **is a blank status reportable?** It is kept today — if blank means "not yet processed" the report is currently wider than intended; (c) the element name, length and the meaning of each code are taken from the instruction, not from DDIC — nothing here can read the view. If `WHLDGTAXITEMSTATUS` is not an element of the view the program will not activate, and the fix is one line in `FETCH_WT_ITEMS`. | Excluded in the driver SELECT; blank kept. |

### Second run, company code with the `19*` / `511*` ranges — 27/08/26

17 rows, **all reconcile, no exceptions.** `4,000 x 0.1% = 4.00`, `175,000 x 2% = 3,500`,
`300,000 x 0.1% = 300.00`, `100,000 x 2% = 2,000`, `450 x 0.1% = 0.45`. No blank GL, no
status message, no rate/amount mismatch.

- **Q18 fully closed** - chart `ASTL` resolves in this company code too, so the FS's
  hardcode holds for both in scope.
- **The GL decision tree is now fully covered.** Run 1 exercised the `MBEW-BKLAS` -> `T030`
  BSX route; this run adds the **`EKKN-SAKTO`** route - rows 15-17 (`5110000001-03`) return
  `60020201 CONSUMPTION PROJECT INVENTORY`, a project/WBS account assignment. Row 14
  (`5110000000`) returns `33010003 STOCK OF TRADED GOODS` via BSX. Both branches proven.
- **Q24 sharpened.** The rate/amount mismatch does not occur anywhere in this run. Together
  with run 1 - where it hit only LEAN SERVICE, a vendor holding an exemption certificate -
  this is strong evidence the mismatch is certificate-driven source data, **not a code
  defect**. The `WT_QSZRT` check on `1900000077` will settle it.
- Minor, source data not the report: rows 9 and 10 carry an Invoice Date of `26.05.2025`
  against a posting date of `26.05.2026`.
